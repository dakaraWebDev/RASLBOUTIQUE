import { NextResponse } from 'next/server';
import React from 'react';
import UnderConstruct from '../../components/UnderConstruct';

const _middleware = () => {
  return NextResponse.UnderConstruct
};

export default _middleware;
